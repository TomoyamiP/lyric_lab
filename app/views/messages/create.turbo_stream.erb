<%= turbo_stream.append "messages" do %>
  <%= render "messages/messages", messages: @song.messages[-3..] %>
<% end %>

<%= turbo_stream.replace "new_message" do %>
  <%= render "messages/form", song: @song, message: Message.new %>
<% end %>
