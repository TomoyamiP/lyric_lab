<ul class="chat" id="messages">
  <% messages.each do |message| %>
    <% unless message.role == 'system' %>
      <li class="chat-bubble <%= message.role %>" data-controller="auto-scroll">
        <%= raw(render_markdown(message.content)) %>
      </li>
    <% end %>
  <% end %>
</ul>
